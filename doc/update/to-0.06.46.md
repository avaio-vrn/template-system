1. move js in client subdir(change require in application.js)
  client/ts_async
  forms -> client/form_trans_label

2. rm chpu.js;

3. rm ru.yml base locales;

4. check template_types(raw && raw-light)
  add css raw
  add settings raw && raw raw-light

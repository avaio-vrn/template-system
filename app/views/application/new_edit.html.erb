<%= content_for :h1, content_tag(:h1, @content_header || t("#{params[:controller]}.#{params[:action]}.header_h1", obj: @admin_panel&.obj_for_t), class: config_get(:html, :h1_class)) %>
<%= content_for(:admin_javascripts, javascript_include_tag('cocoon_add')) if(current_user && config_get(:main, :gems).include?('cocoon')) %>

<div class="template">			
  <%= render (form_partial ||= 'form'), remote_action: false %>
</div>
